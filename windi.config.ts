export default {
  attributify: true,
  theme: {
    extend: {
      backgroundImage: {
        gradient: 'radial-gradient(circle, #888 10%, #444)',
      },
      animation: {
        'spin-slow': 'spin 3s linear infinite',
      },
    },
  },
}
